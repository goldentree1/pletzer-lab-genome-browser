import type { JBrowseCustomConfigHybrid } from './types';
import configJson from '../config-eg-hybrid.json';

const myConfig: { [key: string]: JBrowseCustomConfigHybrid } = configJson;
export default myConfig;
